import { Router } from 'express';

const adoptionsRouter = Router();

adoptionsRouter.get('/', (request, response) => {

});

adoptionsRouter.post('/', (request, response) => {

});

export default adoptionsRouter;
