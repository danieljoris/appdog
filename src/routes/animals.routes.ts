import { Router } from 'express';

const animalsRouter = Router();

animalsRouter.get('/', (request, response) => {
});

animalsRouter.post('/', (request, response) => {
});

export default animalsRouter;
